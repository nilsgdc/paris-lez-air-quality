# Virtual environments
zfe-env/
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data: raw sources are too large / not redistributable.
# Folder structure is committed (via .gitkeep) so users only drop the
# downloaded files into the existing folders. See README "Download raw data".
data/raw/*
!data/raw/airparif/
!data/raw/zfe_perimetres/
!data/raw/insee_iris/

data/raw/airparif/*
data/raw/zfe_perimetres/*
data/raw/insee_iris/*

!data/raw/airparif/stations_metadata.csv
!data/raw/zfe_perimetres/.gitkeep
!data/raw/insee_iris/.gitkeep

# Secrets
.env
*.key
.kaggle/
kaggle.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude
.claude/
